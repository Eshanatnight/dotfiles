local options = {
    ensure_installed = { "lua", "cpp", "yaml", "json", "python", "xml", "toml" },

    highlight = {
        enable = true,
        use_languagetree = true,
    },

    indent = { enable = true },
}

return options
